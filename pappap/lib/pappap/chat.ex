defmodule Pappap.Chat do
  @db_domain_url "http://localhost:4000"
  @api_url "/api"
  @send_chat_url "/chat_room"
  @content_type [{"Content-Type", "application/json"}]

  def send_chat(%{} = params) do
    
  end
end